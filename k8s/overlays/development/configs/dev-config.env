NODE_ENV=development
LOG_LEVEL=warn
CORS_ENABLED=false
# Tip: Because when using an Nginx Ingress, Next.js needs to know it is behind a proxy
TRUST_PROXY=true
# Optional: To disable Next.js telemetry at runtime
NEXT_TELEMETRY_DISABLED=1